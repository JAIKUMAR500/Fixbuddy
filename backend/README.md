# FixBuddy API

Express + MongoDB.

```bash
npm install
copy .env.example .env
npm run dev
```

Uploads are written to `uploads/` and returned as URLs (`/api/uploads/<file>`). SVG is supported. Pasted `https://` image URLs are stored as-is.
