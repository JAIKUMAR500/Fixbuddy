# Baked into the APK at sync time (frontend Vite env).
# Leave unset to use the live Render API.
# VITE_API_URL=https://fixbuddy-1-nh5a.onrender.com
# Emulator → local backend:
# VITE_API_URL=http://10.0.2.2:4000
